# Example : Test using Selenium 

To install Selenium, 

    pip install selenium

To run, 

    python -m unittest filename.py


If already have selenium installed, 
    
    pip install selenium --upgrade